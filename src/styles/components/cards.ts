import styled from '@emotion/styled'

import { Card } from 'packages/velcrostrip/src/Card'

export const ListViewCard = styled(Card)`
  padding: 0.75rem 1.5rem;
`

export const ListViewCardTitle = styled.div`
  font-weight: 500;
`

export const ListViewCardText = styled.div`
  // TODO: no ad-hoc inline colors
  color: #666;
`
